///
/// HybridMrLectureSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `VoiceInfo` to properly resolve imports.
namespace margelo::nitro::mrlecture { struct VoiceInfo; }
// Forward declaration of `Engine` to properly resolve imports.
namespace margelo::nitro::mrlecture { enum class Engine; }
// Forward declaration of `SpeakOptions` to properly resolve imports.
namespace margelo::nitro::mrlecture { struct SpeakOptions; }
// Forward declaration of `ExportJob` to properly resolve imports.
namespace margelo::nitro::mrlecture { struct ExportJob; }
// Forward declaration of `ExportOptions` to properly resolve imports.
namespace margelo::nitro::mrlecture { struct ExportOptions; }

#include <string>
#include "VoiceInfo.hpp"
#include <vector>
#include <NitroModules/Promise.hpp>
#include "Engine.hpp"
#include "SpeakOptions.hpp"
#include "ExportJob.hpp"
#include "ExportOptions.hpp"

namespace margelo::nitro::mrlecture {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `MrLecture`
   * Inherit this class to create instances of `HybridMrLectureSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridMrLecture: public HybridMrLectureSpec {
   * public:
   *   HybridMrLecture(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridMrLectureSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridMrLectureSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridMrLectureSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void setPiperModel(const std::string& onnxPath, const std::string& tokensPath) = 0;
      virtual void setKokoroModel(const std::string& modelPath, const std::string& voicesPath, const std::string& tokensPath) = 0;
      virtual std::shared_ptr<Promise<std::string>> downloadModel(const std::string& engine, const std::string& modelId, const std::string& destDir) = 0;
      virtual std::shared_ptr<Promise<void>> sampleVoice(Engine engine, const std::string& text, const SpeakOptions& options) = 0;
      virtual std::shared_ptr<Promise<std::vector<VoiceInfo>>> getVoices(Engine engine) = 0;
      virtual std::shared_ptr<Promise<void>> speak(const std::string& text, Engine engine, const SpeakOptions& options) = 0;
      virtual std::shared_ptr<Promise<void>> exportBatch(const std::vector<ExportJob>& jobs, Engine engine, const ExportOptions& options) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "MrLecture";
  };

} // namespace margelo::nitro::mrlecture
