///
/// JExportOptions.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "ExportOptions.hpp"

#include <optional>
#include <string>

namespace margelo::nitro::mrlecture {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "ExportOptions" and the the Kotlin data class "ExportOptions".
   */
  struct JExportOptions final: public jni::JavaClass<JExportOptions> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/mrlecture/ExportOptions;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct ExportOptions by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    ExportOptions toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldVoiceId = clazz->getField<jni::JString>("voiceId");
      jni::local_ref<jni::JString> voiceId = this->getFieldValue(fieldVoiceId);
      static const auto fieldRate = clazz->getField<jni::JDouble>("rate");
      jni::local_ref<jni::JDouble> rate = this->getFieldValue(fieldRate);
      static const auto fieldConcurrency = clazz->getField<jni::JDouble>("concurrency");
      jni::local_ref<jni::JDouble> concurrency = this->getFieldValue(fieldConcurrency);
      return ExportOptions(
        voiceId != nullptr ? std::make_optional(voiceId->toStdString()) : std::nullopt,
        rate != nullptr ? std::make_optional(rate->value()) : std::nullopt,
        concurrency != nullptr ? std::make_optional(concurrency->value()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JExportOptions::javaobject> fromCpp(const ExportOptions& value) {
      using JSignature = JExportOptions(jni::alias_ref<jni::JString>, jni::alias_ref<jni::JDouble>, jni::alias_ref<jni::JDouble>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        value.voiceId.has_value() ? jni::make_jstring(value.voiceId.value()) : nullptr,
        value.rate.has_value() ? jni::JDouble::valueOf(value.rate.value()) : nullptr,
        value.concurrency.has_value() ? jni::JDouble::valueOf(value.concurrency.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::mrlecture
